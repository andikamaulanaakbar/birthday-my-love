{
  "name": "birthday-mylove",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "nuxt": "^4.4.2",
    "vue": "^3.5.33",
    "vue-router": "^5.0.6"
  }
}
